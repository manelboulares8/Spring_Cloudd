spring:
  application:
    name: department
  datasource:
    url: 'jdbc:h2:mem:depdb'
  h2:
    console:
      enabled: true
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  cloud:
    config:
      enabled: false
    discovery:
      enabled: false
server:
  port: 8083
management:
  endpoints:
    web:
      exposure:
        include: '*'